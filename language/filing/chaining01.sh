#!/usr/bin/bash

# Shows how to chain commands together using: '&&'...  

# NOTE: This example assumes that 'fileName' already exists inside of the current directory folder...   

mkdir test && mv fileName

