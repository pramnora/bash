#!/usr/bin/bash

# create a new file called fileName1

touch fileName1

# NOTE: In Linux filenames are 'case sensitive'...; 
#       meaning, filename, fileName, FILENAME
#       are NOT the same; but, are 3 separate files, instead
