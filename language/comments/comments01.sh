#!/usr/bin/bash

# This is a single line comment.
# NOTE: A single line comment beings with a hash: (#) symbol.

: ' This is a multi-line comment block.

    NOTE: A multi-line comment block starts with a colon symbol (:)
          which is placed at the very beginning of the line,
	  and, inside of the first column;
	  this, colon symbol is next followed by a single space,
	  and, a single apostrophe symbol.

	  The multi-line comment block is, finally, closed off
	  by using a single apostrope symbol.  
'

echo Comments file
